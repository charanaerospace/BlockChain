// A function is a group of reusable code  which can be  called anywhere in  ypur program
// Eliminates writing the code  again and again
// helps in writing modular codes
// divides a big programmer into a number of  small and manageable function

//Structures
pragma solidity >=0.8.0 <0.9.0;


contract learnFunctions {
     //function name ()parameters
    function remoteControlOpen(bool closedDoor)  public returns(bool) {
    // allcode of function - the script - the statement
    } 
    // local variables superceed satet variables
    uint a = 36;
    // create functio that adds variables   
    function addValues() public view returns(uint) {
        // uint a  = 3;
        uint b  = 5;
        uint c  = a + b;
        return c;
    }  
}